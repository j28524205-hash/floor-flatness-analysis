# 바닥면 평탄성 분석 웹 앱 개발 문서

> 점군 데이터 기반 건축 마감면 품질 측정 알고리즘의 실제 구현체. 한일 심포지엄 논문(`한일심포지엄_영문초안.md`)에서 서술한 알고리즘의 소스 코드이며, 이 문서는 앱 자체의 개발/유지보수 기록이다.

---

## 1. 개요

- **목적**: 3D 점군 데이터를 입력받아 바닥면 평탄도(돌출/함몰/균열)를 자동 측정하고, 웹 UI로 즉시 시각화·보고서 출력
- **실행 방식**: 로컬 Streamlit 앱
- **주요 파일**:
  - `floor_flatness_app.py` — 앱 본체 (알고리즘 + Streamlit UI)
  - `run_flatness.py` — 실행 진입점 (`streamlit run`을 서브프로세스로 호출)
  - `바닥면_평탄성_분석_실행.bat` — 더블클릭 실행용 배치 파일
  - `floor_flatness_analysis.ipynb` — 초기 알고리즘 실험용 노트북

## 2. 실행 방법

```
바닥면_평탄성_분석_실행.bat 더블클릭
→ python run_flatness.py 실행
→ streamlit run floor_flatness_app.py (서브프로세스)
→ 브라우저에서 http://localhost:8501 자동 오픈
```

## 3. 알고리즘 파이프라인 (5단계)

| 단계 | 함수 | 설명 |
|---|---|---|
| 1 | `remove_outliers(points, k=20, std_ratio=2.0)` | SOR 이상치 제거. k-최근접 평균거리 기준 2.0σ 초과 포인트 제거 |
| 2 | `separate_floor(points, z_margin, normal_angle_thresh)` | Z값 임계 + 법선 각도(35° 미만) 필터로 바닥/벽체 분리 |
| 3 | `fit_plane_ransac(points, threshold_m, n_iter=1000, seed=42)` | RANSAC 1,000회 반복으로 기준 평면 추정 |
| 4 | `detect_cracks(points, k=15)` | 곡률(40%)+법선 불연속성(40%)+비등방성(20%) 가중 결합 균열 점수 |
| 5 | UI 내 분류 로직 | KCS 41 46 01(±3mm) + 균열 점수로 A/B1/B2/C 4등급 분류 |

- 판정 기준: 불량 비율 우수(≤5%)/양호(≤15%)/보통(≤30%)/불량(>30%)
- 시각화: 2D 편차 히트맵, 3D 표면(Z축 과장 ×1~×500), 등급별 3D 산점도, 편차 히스토그램
- 출력: 결과 CSV, TXT 요약, PDF 보고서(히트맵+히스토그램+3D 포함)

## 4. 수정 이력

### 2026-07-06
- **`바닥면_평탄성_분석_실행.bat` 경로 버그 수정**: `%USERPROFILE%\Desktop\run_flatness.py`(존재하지 않는 경로)를 참조하던 것을 실제 위치(`Desktop\윤호\2026\RISE 학문후속세대 연구\run_flatness.py`)로 수정. (심포지엄 논문 폴더에 복사해둔 사본 bat 파일에서 발견 후 원본에도 반영)
- **PDF 폰트 포터블리티 개선**: `generate_pdf()`가 `C:/Windows/Fonts/malgun.ttf`를 하드코딩해 Windows 외 환경(추후 클라우드 배포 시 Linux)에서 PDF 생성이 무조건 실패하던 문제. `find_korean_font()` 함수를 추가해 Windows(맑은고딕) → Linux(Noto Sans CJK, 나눔고딕) → macOS(AppleGothic) 순으로 탐색하도록 개선. 폰트를 못 찾으면 명확한 에러 메시지 발생.
- **소규모 점군 방어 코드 추가**: `remove_outliers`가 점 개수보다 큰 k를 요구할 때 크래시하던 문제 수정(k 자동 축소). 분석 실행 버튼 클릭 시 최소 30개 포인트 미만이면 경고만 표시하고 중단, 바닥 분리 후 포인트가 4개 미만이면 에러 안내 후 정지하도록 개선(이전에는 이런 경우 `NearestNeighbors`/RANSAC에서 알 수 없는 예외로 앱이 죽었음).
- **RANSAC 재현성 확보**: `fit_plane_ransac`이 시드 없는 전역 `np.random`을 사용해 동일 데이터 재분석 시마다 결과가 미세하게 달라지던 문제. `seed=42` 고정 파라미터 추가로 재현 가능하게 변경.
- **검증**: `py_compile` 문법 체크, Streamlit 헤드리스 실행 후 HTTP 200 확인, RANSAC 재현성/소규모 점군 처리/폰트탐색 함수 개별 단위 테스트 통과.

### 2026-07-06 (2차 리뷰 반영)
외부 코드 리뷰(및 자체 재검토)로 발견된 6건 추가 수정.
- **[높음] `remove_outliers` 전체 삭제 버그**: 이웃 거리 표준편차가 0(완전 대칭/격자형 배치 등)이면 `mean_d < thresh`가 단 하나도 만족되지 않아 포인트가 전부 제거되던 문제. `std < 1e-12`이면 이상치 제거를 건너뛰도록 수정. 대칭 링 형태 데이터로 검증(수정 전 이론상 0개, 수정 후 40/40개 유지 확인).
- **[높음] `fit_plane_ransac` 퇴화 데이터 크래시**: 1,000회 반복 내내 유효 평면을 못 찾으면 `best_plane=None`으로 `TypeError`가 나던 문제. `None`이면 명확한 `ValueError`를 발생시키도록 수정하고, 호출부(분석 실행 버튼)를 `try/except`로 감싸 사용자에게 친절한 에러 메시지로 표시.
- **[높음] `griddata` 실패 시 앱 크래시**: 2D 히트맵/3D 표면 생성에 쓰이는 `griddata(method="linear")`가 점 배치가 일직선/삼각분할 불가 형태면 예외를 던지던 문제. 각각 `try/except`로 감싸 실패 시 해당 시각화만 건너뛰고 경고 메시지 표시하도록 개선.
- **[중간] RANSAC 인라이어 임계값이 판정 기준과 혼용되던 문제**: 사용자가 조정하는 "평탄도 허용 편차(mm)"가 등급 판정뿐 아니라 RANSAC 기준 평면 추정에도 그대로 쓰여, 판정 기준을 바꾸면 기준 평면 자체가 흔들리던 문제. RANSAC 전용 고정값(`RANSAC_INLIER_MM = 5.0`)으로 분리.
- **[중간] 업로드 데이터 검증 부족**: 컬럼 3개 미만, NaN/Inf, 단위 오인식(mm를 m로 착각) 등을 걸러내지 않던 문제. 컬럼 수 체크, NaN/Inf 포인트 자동 제외 후 경고, Z값 범위가 비정상적으로 크면(>50) 단위 오인식 의심 경고를 추가.
- **[낮음, 코드 변경 없음] 바닥 분리 OR 로직의 한계**: `z_mask | angle_mask` 방식은 바닥 외 다른 수평면(테이블 상판, 계단 디딤판 등)도 바닥으로 오분류할 수 있음. 논문에 서술된 방법론과 동일하게 유지하기 위해 로직은 바꾸지 않고, 함수 docstring과 UI 캡션에 한계를 명시.
- **검증**: `py_compile` 통과, Streamlit 헤드리스 재실행 HTTP 200 확인, `remove_outliers`/`fit_plane_ransac` 수정분 개별 재현 테스트 통과. `한일 심포지엄` 폴더의 사본 파일도 동기화.

### 2026-07-06 (3차: 사본 관리 정리 + PDF 이미지 실패 처리)
- **세 번째 사본 발견**: `한일 심포지엄\평활도앱\` 폴더에 2차 수정 이전 상태의 사본이 있었음(다른 리뷰 도구가 이 사본을 검사해 이미 고친 문제들을 다시 보고). `diff`로 확인 후 최신 버전으로 동기화함.
- **[중간] PDF 차트 이미지 삽입 실패가 조용히 무시되던 문제**: `generate_pdf()`의 `insert_fig()`가 `except Exception: pass`로 실패를 삼켜서, `kaleido` 등 의존성 문제로 이미지 변환이 실패해도 사용자는 차트가 빠진 PDF를 아무 경고 없이 받았음. `RuntimeError`로 재발생시키도록 수정해, 호출부의 "PDF 생성 실패" 경고로 사용자에게 명확히 전달되도록 함.
- **동기화 대상 3곳**: `Desktop\윤호\2026\RISE 학문후속세대 연구\`(원본), `Desktop\한일 심포지엄\`, `Desktop\한일 심포지엄\평활도앱\` — 앞으로 코드 수정 시 이 세 곳을 모두 동기화해야 함.

### 2026-07-06 (4차: RANSAC 정확도 개선 + 추가 견고성/구조 정리)
- **[정확도] RANSAC 최종 평면을 인라이어 전체로 재적합**: 기존엔 최다 인라이어를 만든 무작위 3점 평면을 그대로 최종 결과로 사용했음. 이제 해당 평면의 인라이어 전체를 모아 SVD 기반 최소제곱 평면으로 재적합해 정확도를 높임(3점 샘플의 우연한 오차 감소). 대칭 노이즈 데이터로 재적합 평면이 참값(z=0 평면)에 근접함을 확인.
- **[견고성] 이상치 제거 후 포인트 수 미검증**: `remove_outliers(xyz)` 뒤 `clean`의 길이를 확인하지 않고 바로 `separate_floor`로 넘기던 문제. `run_analysis()` 내부에서 `len(clean) < MIN_POINTS`를 명시적으로 검사해 원인이 분명한 에러 메시지를 내도록 함.
- **[견고성] 단위 오인식 휴리스틱이 Z축만 검사**: 평평한 바닥을 mm 단위로 스캔한 경우 Z 범위 자체는 작아 기존 휴리스틱(Z>50)을 통과할 수 있었음. X/Y 범위(방 규모, >500)도 함께 검사하도록 확장.
- **[견고성] `griddata` 실패 시 대체 시각화 없이 건너뛰기만 함**: `safe_griddata()` 헬퍼를 추가해 `linear` 보간 실패 시 `nearest`로 자동 재시도하도록 개선(히트맵·3D 표면 모두 적용). 그래도 실패하면 기존처럼 경고 후 건너뜀.
- **[구조 정리] `try` + `if True` 임시 래퍼 제거**: 분석 파이프라인(이상치 제거~포인트 분류)을 `run_analysis()` 함수로 추출하고, 버튼 핸들러는 이 함수를 `try/except`로 감싸 호출하는 방식으로 재구성. 이전의 어색한 6~8칸 혼용 들여쓰기와 `if True:` 트릭을 제거하고 표준 들여쓰기로 정리.
- **[정리] 죽은 코드 제거**: 사용되지 않던 `grade_map` 딕셔너리 삭제.
- **검증**: `py_compile` 통과, `run_analysis()`/`fit_plane_ransac()` 재적합 로직 단위 테스트 통과(정상 케이스·퇴화 케이스 모두), Streamlit 헤드리스 실행 HTTP 200 확인, 세 위치 파일 `diff` 동일 확인.

### 2026-07-06 (5차: RANSAC 신뢰도 검증 + 단위 선택 UI + 보간 투명성)
- **[정확도] RANSAC 최소 인라이어 비율 검증 추가**: `best_count`(합의된 점 개수)를 검증 없이 그대로 채택하던 문제. `min_inliers = max(30, n*0.2)` 미만이면 "기준 평면을 안정적으로 추정할 만큼 인라이어가 부족합니다" 에러를 발생시켜, 소수 점으로 만든 신뢰도 낮은 평면이 조용히 기준면이 되는 것을 방지.
- **[견고성] `remove_outliers` 과도한 제거 안전장치**: SOR이 전체의 50% 이상을 제거하면(파라미터/데이터 불일치 가능성) 이상치 제거 없이 원본을 그대로 반환하도록 변경.
- **[기능] RANSAC 인라이어 허용오차를 UI에 노출**: 기존엔 코드에 `RANSAC_INLIER_MM=5.0`으로 고정돼 있던 것을 "🔧 고급 설정" 영역에서 사용자가 조정 가능하도록 노출하고, 보고서(TXT/PDF)에도 사용된 값을 명시해 투명성 확보.
- **[기능] 단위 오인식 문제를 휴리스틱 경고 → 명시적 선택 UI로 전환**: 업로드 시 "좌표 단위: m / mm" 라디오 버튼을 추가해 mm 선택 시 자동으로 1000으로 나눠 m로 환산. m를 선택했는데도 좌표 범위가 비정상적으로 크면 마지막 안전장치로 경고 표시.
- **[투명성] `safe_griddata()`가 사용된 보간 방식을 반환**: `nearest`로 대체된 경우 히트맵/3D 표면에 "선형 보간이 불가능해 최근접 보간을 대신 사용했다"는 캡션을 표시해, 결과가 계단형으로 보이는 이유를 사용자가 알 수 있도록 함.
- **[문서화, 코드 변경 없음] 크랙 민감도의 데이터셋 상대성 명시**: `detect_cracks()`의 `norm01()` 정규화가 데이터셋별 최소/최대 기준이라 `curv_thresh` 값의 실제 의미가 파일마다 다를 수 있음을 docstring과 UI 캡션에 명시(알고리즘은 논문 일치성을 위해 변경하지 않음).
- **검증**: `py_compile` 통과, `run_analysis`(정상 케이스) / `fit_plane_ransac`(무작위 노이즈로 최소 인라이어 미달 케이스) / `safe_griddata`(튜플 반환) / `remove_outliers`(이중 밀도 데이터) 단위 테스트 통과, Streamlit 헤드리스 실행 HTTP 200 확인, 세 위치 파일 `diff` 동일 확인.

### 2026-07-06 (6차: 바닥분리 모드 선택 + 안전 임계값 강화 + 투명성 보강)
- **[기능] 바닥 분리 방식(OR/AND)을 UI에서 선택 가능하도록 변경**: 기본값은 기존과 동일한 OR(논문 기준)로 유지하되, "🔧 고급 설정"에 AND(엄격 방식) 옵션을 추가. 테이블 상판·계단 디딤판 등 비바닥 수평면이 섞인 스캔에서 사용자가 직접 더 엄격한 분리를 선택할 수 있음. `separate_floor(..., mode="OR"|"AND")`, `run_analysis(..., floor_mode=...)`로 반영, 보고서에도 사용된 방식 표시.
- **[정확도] RANSAC 최소 인라이어 비율 상향**: `min_inlier_ratio` 기본값을 0.2 → 0.3으로 상향해, 20%만 맞아도 통과하던 느슨한 기준을 강화.
- **[정확도] 이상치 제거 안전 임계값 강화**: `remove_outliers`가 절반(50%) 넘게 제거할 때만 원본을 반환하던 것을 30% 초과로 강화(`max_removal_ratio=0.3`). 또한 함수가 이제 `(정제된 포인트, 제거 비율)` 튜플을 반환해, 실제 제거 비율을 화면 캡션과 보고서에 표시.
- **[투명성] 크랙 점수 상대성 안내를 TXT/PDF 보고서 본문에도 추가**: 기존엔 UI 캡션에만 있던 안내("크랙점수는 스캔 내부 상대 지표")를 보고서 텍스트에도 포함시켜, 보고서만 공유해도 오해 소지가 없도록 함.
- **[투명성] 업로드 데이터의 단위 처리 이력을 세션에 저장**: `st.session_state.xyz_unit_note`에 선택된 단위를 저장하고, 로드 성공 메시지 및 현재 데이터 안내 문구에 표시. 부수적으로 발견한 기존 버그(업로드한 데이터에도 항상 "샘플 데이터"라고 표시되던 문제)도 함께 수정.
- **검증**: `py_compile` 통과, `run_analysis`(OR/AND 모드 각각) / `separate_floor`(모드별 결과) / `remove_outliers`(튜플 반환) 단위 테스트 통과, Streamlit 헤드리스 실행 HTTP 200 확인, 세 위치 파일 `diff` 동일 확인.

### 2026-07-06 (7차: 발견 가능성 개선 + 진단 정보 세분화)
- **[UX] 바닥 분리 방식(OR/AND) 선택을 "고급 설정" 밖, 항상 보이는 위치로 이동**: 접힌 expander 안에 있으면 놓치기 쉽다는 지적을 반영해, "🧱 벽·벽체 자동 분리" 섹션에 바로 노출. 기본값은 여전히 OR(논문 기준)로 변경 없음.
- **[기능] RANSAC 최소 인라이어 비율(%)을 고급 설정에 노출**: 기존엔 코드 상수(`min_inlier_ratio=0.3`)로 고정돼 있던 것을 UI에서 조정 가능하게 하고(`min_inlier_pct` → 0~1 비율 변환), 보고서에도 사용된 값을 기록.
- **[정확도] `remove_outliers` 반환값에 상태(status) 추가**: 기존엔 "제거 안 함"과 "제거 예상 비율이 과다해 안전장치 발동"이 똑같이 `removed_ratio=0.0`으로 보여서 구분이 안 됐음. `(points, removed_ratio, status)`로 변경(`status`: `"ok"`/`"no_outliers"`/`"skipped_too_many"`), 안전장치 발동 시 실제 예상 제거율과 함께 명확한 경고를 표시.
- **[투명성] CSV 컬럼명에 크랙점수의 상대적 성격 명시**: `"크랙점수"` → `"크랙점수_상대(파일내0-1)"`로 컬럼명 변경. CSV만 따로 공유돼도 절대 비교 가능한 값으로 오해되지 않도록 함.
- **[투명성] TXT/PDF 보고서에 좌표 단위 변환 이력 추가**: "입력 좌표 단위"(사용자가 선택한 값)와 "처리 좌표 단위"(항상 m)를 보고서 헤더에 명시.
- **검증**: `py_compile` 통과, `run_analysis`(min_inlier_ratio 전달 확인) / `remove_outliers`(3-튜플, 이중 밀도 데이터의 status 확인) 단위 테스트 통과, Streamlit 헤드리스 실행 HTTP 200 확인, 세 위치 파일 `diff` 동일 확인.

### 2026-07-06 (8차: 남은 하드코딩 파라미터 노출)
- **[기능] 이상치 제거 민감도(`std_ratio`)를 고급 설정에 노출**: 기존 코드 상수(2.0 고정)를 UI에서 조정 가능하게 변경. `run_analysis(..., sor_std_ratio=...)` → `remove_outliers(..., std_ratio=...)`로 전달, 보고서에도 기록.
- **[기능] RANSAC 반복 횟수(`n_iter`)를 고급 설정에 노출**: 기존 코드 상수(1000 고정)를 UI에서 조정 가능하게 변경. 포인트 수가 많거나 이상치 비율이 높은 데이터에서 사용자가 직접 늘릴 수 있음.
- **[재확인, 변경 없음] 바닥 분리 기본값(OR) 유지**: 논문 서술과의 일치성을 위해 유지하기로 한 기존 결정을 재확인함(이번이 4번째 지적). 대신 이미 눈에 띄는 위치로 AND 옵션을 옮겨둔 상태.
- **[재확인, 변경 없음] 크랙 점수 상대 정규화 방식 유지**: docstring/UI 캡션(2곳)/보고서/CSV 컬럼명에 이미 명시돼 있음. 알고리즘을 절대 기준으로 바꾸면 논문에 보고된 결과와 달라지므로 보류.
- **검증**: `py_compile` 통과, `run_analysis`에 `sor_std_ratio`/`ransac_n_iter` 커스텀 값 전달 테스트 통과, Streamlit 헤드리스 실행 HTTP 200 확인, 세 위치 파일 `diff` 동일 확인.

### 2026-07-06 (9차: Streamlit Community Cloud 배포)
- **git 저장소화**: `RISE 학문후속세대 연구` 폴더를 git 저장소로 초기화(로컬 전용 user.name/email 설정). 대용량 pptx(224MB/140MB)와 행정문서 폴더는 `.gitignore`로 제외.
- **GitHub 공개 저장소 생성 및 push**: https://github.com/j28524205-hash/floor-flatness-analysis
- **배포**: Streamlit Community Cloud에서 `floor_flatness_app.py`를 메인 파일로 배포. 앱 URL: `https://floor-flatness-analysis-jumjfkekdybz8lph3at9mk.streamlit.app`
- **`requirements.txt`/`packages.txt` 신규 추가**: 배포 전엔 없던 파일. `packages.txt`에 `fonts-nanum` 추가(Linux 컨테이너에서 `find_korean_font()`가 기대하는 `/usr/share/fonts/truetype/nanum/NanumGothic.ttf` 경로를 제공).
- **[버그] `requirements.txt` 최초 버전의 의존성 충돌**: 로컬 환경 버전을 `==`로 그대로 고정(`numpy==2.2.6`, `pandas==3.0.3` 등)했는데, `pandas==3.0.3`이 실제로는 `numpy>=2.3.3`을 요구해 배포 서버(uv 리졸버)에서 "No solution found" 오류 발생 + pip가 numpy를 소스 빌드하려다 지연. 최소버전 제약(`>=`)으로 완화하여 해결.
- **[버그] `kaleido>=1.0`이 PDF 차트 이미지 삽입 실패시킴**: kaleido 1.x부터 정적 이미지 렌더링에 시스템 Chrome이 별도로 필요(`plotly_get_chrome`)한데, Streamlit Cloud 컨테이너엔 Chrome이 없어 실패. **이 실패가 조용히 무시되지 않고 명확한 에러로 사용자에게 표시된 것은 3차 수정(`insert_fig` 예외 재발생 처리)의 효과가 실제 배포 환경에서 그대로 작동한 것.** `kaleido==0.2.1`(자체 번들 Chromium, 별도 브라우저 불필요)로 하향 고정하여 해결.
- **검증**: 배포된 실제 앱에서 분석 실행 → TXT 저장 정상 확인. PDF 저장은 kaleido 수정 후 재확인 필요(사용자 확인 대기).

`한일심포지엄_영문초안.md`(2장 알고리즘 개발) 서술과 코드를 대조한 결과, 파라미터(k=20/2.0σ, 35°, RANSAC 1000회, 균열 점수 가중치 40/40/20%, KCS 41 46 01 ±3mm)와 부호 규약(양수=돌출/음수=함몰) 모두 일치함을 확인함.

## 6. 알려진 제한사항 / 향후 과제

- `separate_floor`의 Z값+법선각도 결합 방식은 바닥 외에 다른 수평면(테이블 상판, 계단 디딤판 등)이 있으면 오분류 가능 (논문에도 기술된 방법론상의 단순화이며, 별도 개선 없이 현재 범위로 진행)
- `separate_floor`가 포인트마다 Python for 루프로 SVD를 수행해 대규모 점군(수십만 점 이상)에서는 느릴 수 있음 (성능 최적화는 향후 과제)
- 실측 점군 데이터로는 아직 검증하지 않음 (현재는 논문용 가상 데이터로만 검증, `한일 심포지엄/figures/` 생성 스크립트 참고)
- **웹 배포는 별도 트랙**: Streamlit은 지속 실행 서버가 필요해 Firebase Hosting(정적 전용)과는 맞지 않음. 배포 필요 시 Cloud Run 또는 Streamlit Community Cloud 사용 예정 (아직 미착수)

## 7. 관련 문서

- `C:\Users\user\Desktop\한일 심포지엄\한일심포지엄_영문초안.md` — 심포지엄 제출용 논문(영문)
- `C:\Users\user\Desktop\한일 심포지엄\한일심포지엄_국문초안.md` — 국문 검토용 초안
- `C:\Users\user\Desktop\한일 심포지엄\PRD.md` — 논문/알고리즘 프로젝트 PRD
- `C:\Users\user\Desktop\한일 심포지엄\figures\` — 논문용 Figure 1/2/3 생성 결과물
